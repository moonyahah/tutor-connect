<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SEProject">

        <activity android:name=".activities.LoginActivity" />
        <activity android:name=".activities.RequestSessionActivity" />
        <activity android:name=".activities.SignupActivity" />
        <activity android:name=".activities.EditStudentProfileActivity" />
        <activity android:name=".activities.EditTutorProfileActivity" />
        <activity android:name=".activities.AdvisorDashboardActivity" />
        <activity android:name=".activities.StudentDashboardActivity" />
        <activity android:name=".activities.StudentSessionsActivity" />
        <activity android:name=".activities.NotificationsActivity" />
        <activity android:name=".activities.TutorDashboardActivity" />
        <activity android:name=".activities.TutorProfileActivity" />
        <activity android:name=".activities.CreateTutorProfileActivity" />
        <activity android:name=".activities.TutorEarningsActivity" />

        <activity
            android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>